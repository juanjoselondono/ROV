twist_mux:
  ros__parameters:
    topics:
      joystick  : 
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 100

      pc  : 
        topic   : cmd_vel
        timeout : 0.5
        priority: 100